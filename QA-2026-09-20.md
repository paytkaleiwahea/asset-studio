# QA pass: 2026-09-20

Scope: lightweight previews and explicit Save and preview, based on aa47acf.

## Passed this run

- Node/server syntax and doctor: Node 24.14.0, Chrome, FFmpeg/FFprobe 8.1, HyperFrames 0.8.34.
- Editor regression: fresh drafts are independent from applied values; editing does not render; Save renders once; saved state and session drafts reload; exports use applied values.
- Brand regression: starter defaults, fonts, validation, independent overrides.
- Build regression: failed build retains the working manifest, subsequent recovery succeeds.
- Setup regression: repeat defaults preserve configuration; hidden starters survive; excluded media can be restored; invalid input does not overwrite configuration.
- Three repeated motion PNG exports produced identical SHA-256 hashes.
- MP4: H.264, 1080x960, four seconds. Frame at two seconds visually inspected: both lines and caption visible.
- MOV: ProRes, 1080x960, four seconds, alpha-capable pixel format. Frame visually inspected. Alpha at two seconds: 1,015,335 fully transparent and 11,915 fully opaque pixels of 1,036,800.
- Screenshot browser lifecycle with a three-second test timeout: zero child processes while idle, one after new capture, zero after five seconds. Production default remains 60 seconds.

## Boundaries

Previous browser QA verified Save and size switching; previous upload/PNG QA covered all nine starter sizes. Those full UI cases were not repeated in this run. This run adds automated coverage and the previously outstanding video/lifecycle checks.

No laptop call/screen-share benchmark was performed. Passing these checks does not prove the user's call stutter is resolved. Video exports still run separately from the screenshot queue. Saved edits remain browser-local, not portable project files. Existing user changes to studio.config.json were preserved and excluded from this commit.
